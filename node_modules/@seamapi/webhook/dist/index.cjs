'use strict';

var svix = require('svix');

var __accessCheck = (obj, member, msg) => {
  if (!member.has(obj))
    throw TypeError("Cannot " + msg);
};
var __privateGet = (obj, member, getter) => {
  __accessCheck(obj, member, "read from private field");
  return getter ? getter.call(obj) : member.get(obj);
};
var __privateAdd = (obj, member, value) => {
  if (member.has(obj))
    throw TypeError("Cannot add the same private member more than once");
  member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
};
var __privateSet = (obj, member, value, setter) => {
  __accessCheck(obj, member, "write to private field");
  setter ? setter.call(obj, value) : member.set(obj, value);
  return value;
};
var _webhook;
var SeamWebhook = class {
  constructor(secret) {
    __privateAdd(this, _webhook, void 0);
    __privateSet(this, _webhook, new svix.Webhook(secret));
  }
  verify(payload, headers) {
    const normalizedHeaders = Object.fromEntries(
      Object.entries(headers).map(([key, value]) => [key.toLowerCase(), value])
    );
    return __privateGet(this, _webhook).verify(payload, normalizedHeaders);
  }
};
_webhook = new WeakMap();

Object.defineProperty(exports, "SeamWebhookVerificationError", {
  enumerable: true,
  get: function () { return svix.WebhookVerificationError; }
});
exports.SeamWebhook = SeamWebhook;
//# sourceMappingURL=out.js.map
//# sourceMappingURL=index.cjs.map